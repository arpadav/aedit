1. I want zellij tabs to show `broot` and `hx` instead of the bash scripts
2. somehow suppress Bye from Zellij? piping to dev/null doesnt work, since the entire terminal freezes
3. upon opening a file directly from ae, highlight/attention/context is on broot instead of hx
